{
  "name": "atenews-parse-dashboard",
  "version": "1.0.0",
  "description": "test",
  "main": "index.js",
  "scripts": {
    "start": "node ./index.js --config ./parse-dashboard-config.json --allowInsecureHTTP=1",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "parse-dashboard": "^2.1.0"
  }
}
